package com.softserve.edu.opencart.pages.user.mail;

import org.openqa.selenium.WebDriver;

public class IncomingMailPage extends MailSidebarPart {

    public IncomingMailPage(WebDriver driver) {
        super(driver);
        initElements();
    }
    private void initElements() {
        // init elements
    }
}
